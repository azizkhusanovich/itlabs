import { createStore } from "vuex";
import fieldsModule from "./modules/fields";

export default createStore({
  modules: { fields: fieldsModule },
  state: {},
  getters: {},
  mutations: {},
  actions: {},
  modules: {},
});
